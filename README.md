<div align="center">
  <img src="assets/img/logo.png" alt="زاجل إكسبرس" width="300">
  <h1>موقع زاجل إكسبرس للتوصيل</h1>
  <p>موقع تعريفي عربي (RTL) لشركة توصيل — HTML/CSS/JS ثابت بدون أي مكتبات أو أدوات بناء.</p>
</div>

---

## ✨ المزايا

- **عربي بالكامل** باتجاه RTL صحيح، وخط `Tajawal` مع بدائل من خطوط النظام.
- **متجاوب** من عرض ٣٢٠ بكسل حتى الشاشات الكبيرة (تصميم الموبايل أولاً).
- **بدون تبعيات**: لا npm ولا build — افتح الملف واشتغل.
- **هوية مبنية على اللوغو**: لون البراند `#FF503F` مسحوب من اللوغو نفسه.
- **جاهز لمحركات البحث**: وصف لكل صفحة، Open Graph، بيانات منظّمة JSON-LD، `sitemap.xml`، `robots.txt`.
- **إمكانية وصول**: تنقّل بالكيبورد، سمات `aria`، احترام `prefers-reduced-motion`، تباين لوني مدروس.

## 📁 بنية المشروع

```
index.html              الرئيسية (هيرو، أرقام، نبذة، خدمات، مزايا، خطوات، شركاء)
about.html              من نحن: القصة، الرؤية، الرسالة، الأهداف، الخطط، التوجهات، القيم
faq.html                الأسئلة الشائعة (أكورديون + بيانات FAQ منظّمة)
blog.html               قائمة المقالات
blog/post-1..3.html     ثلاث مقالات جاهزة
contact.html            نموذج تواصل + معلومات الاتصال
assets/css/style.css    كل الستايل (المتغيّرات بأعلى الملف)
assets/js/main.js       القائمة، الأكورديون، الحركات، العدّادات، نموذج واتساب
assets/img/             اللوغو والأيقونات والرسومات
robots.txt · sitemap.xml · favicon.png · .nojekyll
```

## 🚀 التشغيل محلياً

أي واحدة من هذي كافية:

```bash
# الأسهل: افتح index.html بالمتصفح مباشرة
# أو شغّل سيرفر محلي:
python3 -m http.server 8000
# ثم افتح: http://localhost:8000
```

## 🎨 تغيير الألوان

كل ألوان الموقع بمكان واحد — أول `assets/css/style.css` داخل `:root`:

```css
:root{
  --brand:      #FF503F;   /* لون اللوغو — الأزرار والأيقونات */
  --brand-600:  #ED4130;   /* درجة أغمق للتأثيرات */
  --brand-soft: #FFF1EE;   /* خلفيات فاتحة */
  --ink:        #14171C;   /* النصوص والأقسام الداكنة */
  --bg:         #FBF9F7;   /* خلفية الصفحة */
}
```

تبديل `--brand` لوحده يغيّر مظهر الموقع كله.

## 🖼️ تغيير اللوغو

استبدل هذي الملفات بنفس الأسماء (يفضّل PNG بخلفية شفافة):

| الملف | الاستخدام |
|---|---|
| `assets/img/logo.png` | الهيدر (خلفية فاتحة) |
| `assets/img/logo-light.png` | القائمة والفوتر (خلفية داكنة) |
| `assets/img/mark.png` | حرف Z لوحده (زخرفة الهيرو) |
| `assets/img/icon-512.png` · `apple-touch-icon.png` · `favicon.png` | أيقونات المتصفح والهاتف |
| `assets/img/og-cover.png` | صورة المشاركة بالسوشيال (1200×630) |

## ☎️ تغيير معلومات التواصل

ابحث واستبدل هذي القيم بكل ملفات `.html` (وبملف `assets/js/main.js`):

| القيمة الحالية (نموذجية) | وين تنحط |
|---|---|
| `9647700000000` | روابط `wa.me` و `tel:` + المتغيّر `SITE.whatsapp` بملف `main.js` |
| `+964 770 000 0000` | النص الظاهر للرقم |
| `info@zajel-express.com` | روابط البريد + `SITE.email` بملف `main.js` |
| `بغداد — العراق` | العنوان بالفوتر وصفحة اتصل بنا |
| `href="#"` بقسم `socials` | روابط فيسبوك وإنستغرام وتيليغرام بالفوتر |

أمر سريع للاستبدال:

```bash
grep -rl "9647700000000" . --include="*.html" --include="*.js" | xargs sed -i "s/9647700000000/رقمك_هنا/g"
```

## ⚠️ محتوى نموذجي لازم تبدّله

الموقع جاهز شكلاً، بس هذي أرقام ونصوص **افتراضية** حطيناها كمثال — بدّلها بمعلوماتك الحقيقية قبل النشر:

- **أرقام الإنجازات** بالرئيسية (`+٥٠٬٠٠٠ طلب`، `١٩ محافظة`، `+١٢٠ مندوب`، `٩٨٪`) — داخل قسم `<section class="stats">` بملف `index.html`.
- **تواريخ المقالات** بملف `blog.html` وصفحات `blog/`.
- **الدومين** `https://zajel-express.com` بملفي `sitemap.xml` و `robots.txt`.
- **روابط canonical و og:url** — مكانها معلّم بتعليق داخل `<head>` بكل صفحة.
- **العنوان وأوقات الدوام** إذا تختلف عن المكتوب.

## 📨 نموذج التواصل

الموقع ثابت وما بيه سيرفر، فنموذج «اتصل بنا» **يجمّع البيانات ويفتح محادثة واتساب** برسالة جاهزة (مع زر بديل يرسل بالبريد). ما تحتاج أي خدمة خارجية ولا اشتراك.

لو حبيت تستقبل الرسائل بالبريد تلقائياً، تكدر تربط النموذج بخدمة مثل Formspree بتغيير `action` و `method` بالعنصر `<form id="contactForm">`.

## 🌍 النشر على GitHub Pages (مجاناً)

1. ادخل على صفحة الريبو → **Settings** → **Pages**.
2. بخانة **Source** اختر `Deploy from a branch`.
3. اختر الفرع المطلوب والمجلد `/ (root)` ثم **Save**.
4. بعد دقيقة راح يطلع لك رابط الموقع بنفس الصفحة.

الموقع ملفات ثابتة، فينشر بنفس السهولة على Netlify أو Vercel أو أي استضافة عادية (ارفع الملفات كما هي).

## 🧪 اللي تم فحصه

- كل الروابط والملفات الداخلية تفتح بدون أخطاء (٢١ رابط).
- لا يوجد سكرول أفقي بعرض ٤٠٢ بكسل ولا ١٣٦٦ بكسل.
- قائمة الموبايل: تفتح وتنغلق بالزر وبمفتاح `Esc`.
- الأكورديون يشتغل وتتحدث سمة `aria-expanded`.
- نموذج التواصل يبني رابط واتساب صحيح بالمعلومات المدخلة.
- console المتصفح خالي من الأخطاء.
